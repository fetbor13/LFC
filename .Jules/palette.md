## 2024-05-24 - Accessibility for Modals and Forms
**Learning:** Custom modals implemented in the project must include `role="dialog"`, `aria-modal="true"`, and `aria-labelledby` attributes to be accessible. Form inputs lacking visible labels (relying only on placeholders) must use `aria-label`.
**Action:** When creating new modals or forms, ensure these attributes are included.
