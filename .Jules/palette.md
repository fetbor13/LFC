## 2024-05-24 - Accessibility for modals
**Learning:** Custom modals implemented in the project must include `role="dialog"`, `aria-modal="true"`, and `aria-labelledby` attributes for accessibility.
**Action:** When creating or modifying modals in the application, make sure they have these attributes. Also, ensure close buttons have `aria-label="Fermer"` in French interfaces.
